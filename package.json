{
  "name": "jobjila",
  "version": "2.0.0",
  "description": "Jobjila — learn a skill, get verified, start earning. Static site generated from data/courses.json.",
  "main": "index.html",
  "scripts": {
    "build": "node scripts/build.js",
    "og": "node scripts/og-images.js",
    "start": "npm run build && live-server . --port=5500",
    "serve": "live-server . --port=5500",
    "test": "node scripts/build.js && echo \"Build OK\""
  },
  "author": "Abhinaw Jaiswal",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
